const CONFIG = {
  database: {
    host: "localhost",
    user: "root",
    password: "admin",
    db: "todoApp",
    dialect: "mysql",
    logging: false,
    pool: {
      max: 5,
      min: 0,
      acquire: 30000,
      idle: 10000
    }
  }
};
module.exports = CONFIG